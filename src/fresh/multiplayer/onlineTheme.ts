import { theme } from '../shared/luxuryTheme';

export const onlineTheme = {
  gold: theme.gold,
  hostColor: theme.player1Color,
  guestColor: theme.player2Color,
  hostTileColor: theme.player1Tile,
  guestTileColor: theme.player2Tile,
  background: theme.deepBrown,
  panel: theme.onlinePanel,
  panelStrong: theme.onlinePanelStrong,
  panelBorder: theme.goldBorderSoft,
  text: theme.creamText,
  mutedText: theme.mutedText,
  codeDisplay: theme.gold,
  errorText: theme.errorText,
  inputBackground: theme.onlineInput,
};
